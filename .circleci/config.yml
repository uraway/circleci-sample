version: 2.1
jobs:
  build:
    parameters:
      node:
        type: enum
        enum: ["node", "node-browsers", "node-browsers-legacy"]
        default: node
    docker:
      - image: circleci/python:3.7-<<parameters.node>>
    steps:
      - run: echo <<parameters.node>>
