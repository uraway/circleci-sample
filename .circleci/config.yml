version: 2.1
jobs:
  build:
    docker:
      - image: busybox
    steps:
      - run: circleci -v
      - run: circleci step halt
      - run: circleci -v

workflows:
  version: 2
  main:
    jobs:
    - build
