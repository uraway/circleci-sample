version: 2.1
jobs:
  build:
    machine: true
    steps:
      - when:
          condition:
            or:
              - not:
                - equal: [ development, $CIRCLECI_BRANCH ]
              - and: [ $CIRCLE_TAG ]
          steps:
            - run: echo "deploy"
