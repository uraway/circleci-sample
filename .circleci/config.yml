version: 2

jobs:
  build:
    machine:
      image: circleci/ubuntu-1604:201903-01
    steps:
      - run:
          echo 'Hello World' >> README.md
          cat README.md

workflows:
  version: 2
  "Hello Workflow":
    jobs:
      - build
