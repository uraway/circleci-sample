version: 2.1
jobs:
  build:
    machine: true
    steps:
      - when:
          condition:
            or:
              - not:
                - equal: [ development, $CIRCLE_BRANCH ]
          steps:
            - run: echo "deploy"
